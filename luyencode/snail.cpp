#include <iostream>
using namespace std;

int main ()
{
    long long a, b, v;
    cin >> a >> b >> v;
    if (a == v) {
        cout<<"1";
        return 0;
    } else {
        if ((V-B)%(A-B)==0) cout<<(V-B)/(A-B);
        else cout<<(V-B)/(A-B)+1;
    }
    return 0;
}
